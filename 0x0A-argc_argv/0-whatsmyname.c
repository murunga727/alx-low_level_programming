#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * @argc: an int
 * @argv: pointer to a list
 * Return: 0(success)
 */
int main(int argc, char *argv[])
{

	printf("%s\n", argv[argc * 0]);
	return (0);
}
